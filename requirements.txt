# Exact versions used to produce the results in the manuscript.
# Tested with Python 3.12.5 on Windows 11.
# Looser bounds that are also expected to work are given in README.md.
numpy==2.2.3
pandas==2.2.3
shapely==2.1.1
matplotlib==3.10.0
seaborn==0.13.2
PyYAML==6.0.2
